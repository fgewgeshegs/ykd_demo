package com.youkeda.exercise.claw.budget;

/** 团建方案费用项目的计费方式。 */
public enum PricingMode {
    FIXED,
    PER_PERSON,
    PER_PERSON_PER_DAY,
    PER_PERSON_PER_OCCURRENCE,
    PER_ROOM_PER_NIGHT,
    PER_VEHICLE,
    PER_TABLE,
    PER_UNIT
}
